spring.application.name=JSON XML editor
server.port=${PORT:8085}
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always
baseurl=${BASE_URL}

# Mail Configuration (optional, configured via environment variables)
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:jsonxmleditor@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:rnge vbuc wksr fkxt}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
